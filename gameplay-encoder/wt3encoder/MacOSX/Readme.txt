To build the encoder:

- compile it with xcode and copy the binary to this folder
- copy the converter to this folder

Create a binary from the shell script:
- install shc
- shc -f wt3encoder.sh
- rename wt3encoder.sh.x to wt3encoder